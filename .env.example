# API Keys & Configuration - copy this file to .env and fill in your values
# Summarizer API Keys
GEMINI_API_KEY=""
OPENROUTER_API_KEY=""

# Ollama Configuration
OLLAMA_API_BASE_URL="http://localhost:11434" # Default if Ollama is local

# Azure TTS Configuration
AZURE_TTS_API_KEY=""
AZURE_TTS_REGION=""

# Google Cloud TTS Credentials
# Note: For Google Cloud TTS, python-dotenv doesn't directly handle file paths embedded in strings
# as dynamic variables in the same way. The application will expect this env var to be the *path* string.
GOOGLE_CLOUD_TTS_CREDENTIALS_PATH=""

# Minimax TTS Configuration
MINIMAX_API_KEY=""
MINIMAX_GROUP_ID=""

# Other configurations can be added here if needed
